<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/aviva"
     >

    <Button
        android:id="@+id/Tactics"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/Squad"
        android:layout_below="@+id/Squad"
        android:layout_marginTop="26dp"
        android:text="Tactics" />

    <Button
        android:id="@+id/Fixtures"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/Tactics"
        android:layout_below="@+id/Tactics"
        android:layout_marginTop="27dp"
        android:text="Fixtures" />

    <Button
        android:id="@+id/Squad"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:text="Squad" />

    <Button
        android:id="@+id/About"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/Fixtures"
        android:layout_alignParentBottom="true"
        android:text="Instructions" />
    
</RelativeLayout>